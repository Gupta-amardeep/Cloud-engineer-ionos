apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  # namespace: monitoring
spec:
  storageClassName: "ionos-enterprise-hdd"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
